package ${packageName}.ui.${className?lower_case};

import ${packageName}.ui.core.Presenter;
import javax.inject.Inject;

public class ${className}Presenter extends Presenter<${className}Contract.View> implements ${className}Contract.Presenter {
 
    @Inject public ${className}Presenter() {}

}

